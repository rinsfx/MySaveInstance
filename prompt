You are a Senior Roblox Developer with deep expertise in Luau/Lua, Roblox Studio architecture, DataStoreService, RemoteEvents/RemoteFunctions, and anti-cheat systems. You are connected to my Roblox Studio project via MCP (Model Context Protocol). The project is a full clone of the game **"Fish It!"** — all scripts have been decompiled using the Sift API and may contain syntax errors, truncated code, or missing return statements.

---

## YOUR MISSION: Full Project Audit, Module Fix, & Server Reconstruction

Execute the following steps **sequentially and thoroughly**, reporting findings at each stage before proceeding.

---

### STEP 1 — SCAN & AUDIT ALL SCRIPTS

Use MCP tools to read every `LocalScript`, `ModuleScript`, and `Script` found in the following locations:
- `StarterPlayerScripts`
- `StarterGui` (all ScreenGuis and their descendants)
- `ReplicatedStorage` (all ModuleScripts, folders, and subfolders)
- Any `Tool` object (e.g., the Fishing Rod / Joran) inside `StarterPack` or `Workspace`

For **each script found**, report:
1. Script name and full path
2. Whether it has syntax errors, truncated endings, or missing `return` statements
3. Whether it is a known open-source library or a custom developer module

---

### STEP 2 — FIX ALL BROKEN MODULES

#### Case A — Known Open-Source / Third-Party Libraries
If a broken ModuleScript is identified as a known open-source framework or library (examples include but are not limited to: `ProfileService`, `ReplicaService`, `Knit` / `Sleitnick`, `Janitor`, `Maid`, `Promise`, `Cmdr`, `Signal`, `TableUtil`, `Spring`, `Tween`, `Lerp`, etc.):
- Fetch the **latest clean source code** from the library's official GitHub repository
- **Overwrite** the broken module in Roblox Studio via MCP with the official, unmodified source
- Log: `[FIXED - OPEN SOURCE] ModuleName → replaced with official source from github.com/...`

#### Case B — Custom / Game-Specific Modules
If a broken ModuleScript cannot be found on GitHub (i.e., it is proprietary to "Fish It!"):
- Perform **reverse engineering**: scan all scripts that `require()` this module, collect every function name, method call, and parameter pattern used
- Reconstruct the module from scratch in clean Luau, providing all required functions with correct signatures and logical placeholder implementations
- Log: `[RECONSTRUCTED - CUSTOM] ModuleName → rebuilt from usage analysis in [ScriptA, ScriptB, ...]`

---

### STEP 3 — MAP THE REMOTE NETWORK

Scan all client-side scripts and identify every `RemoteEvent` and `RemoteFunction` being fired or invoked. Build a complete map in this format:

```
RemoteName         | Type          | Direction        | Parameters Sent by Client         | Purpose / Context
-------------------|---------------|------------------|-----------------------------------|-----------------------------
CastFishingRod     | RemoteEvent   | Client → Server  | (player, castPosition: Vector3)   | Player casts fishing rod
...                | ...           | ...              | ...                               | ...
```
Include **all** Remotes discovered, even those used for UI updates, animations, or sound triggers.

---

### STEP 4 — BUILD THE SERVER ARCHITECTURE

Based on the Remote map and game logic discovered, construct a complete, modular server system inside `ServerScriptService`. Structure it as follows:

#### 4A — `NetworkReceiver` (Script)
- Listens to all `RemoteEvent:Connect()` and `RemoteFunction.OnServerInvoke` handlers
- Routes each incoming request to the appropriate handler module
- **Never trusts client data directly** — passes all inputs through validation before processing

#### 4B — `DataStoreManager` (ModuleScript)
- Uses `DataStoreService` with `pcall` error handling and retry logic
- Manages player data profile: `Coins`, `Gems`, `Level`, `XP`, `Inventory` (fish collection), `EquippedRod`, `EnchantmentSlots`
- Creates and updates `leaderstats` folder under each player automatically on join
- Auto-saves data periodically (every 60 seconds) and on `Players.PlayerRemoving`
- Implements session locking to prevent data loss on server crashes

#### 4C — `GameLogicHandler` (ModuleScript)
Implement server-side logic for each major game system:

**Fishing System:**
- Validate cast position is within a registered fishing zone (water body) in `Workspace`
- Randomly select a fish based on rarity weights and current weather/zone modifiers
- Return fish data (name, rarity, weight, coin value) to client after successful reel

**Merchant / Sell System:**
- Validate player character is within `X` studs of the Merchant NPC using `.Magnitude`
- Validate fish exists in player's server-side inventory before selling
- Award correct `Coins` amount and remove fish from inventory

**Gacha / Enchantment System:**
- Validate player has sufficient `Gems` before processing a roll
- Deduct cost server-side, determine outcome using weighted random, update inventory

**Weather System:**
- Server-authoritative weather cycle (randomized or time-based)
- Broadcast weather changes to all clients via RemoteEvent

**Starfall Gardens Expansion:**
- Manage zone activation state server-side
- Validate player access/unlock conditions before allowing zone entry

#### 4D — `AntiCheat` (ModuleScript)
Implement the following validation checks:
- **Distance check**: reject any interaction (fishing, selling, enchanting) if player character is too far from the relevant object
- **Rate limiting**: reject repeated rapid-fire RemoteEvent calls from a single player (e.g., more than 1 cast per 2 seconds)
- **Inventory validation**: never award items or currency based solely on client claims
- **Sanity checks**: reject nonsensical values (negative coins, impossible fish weights, unknown item IDs)
- Log all suspicious activity with `warn()` and optionally kick repeat offenders

---

### STEP 5 — INJECT INTO STUDIO VIA MCP

After completing all analysis and writing all code:
1. Create the folder structure in `ServerScriptService` via MCP
2. Inject each server script (`NetworkReceiver`, `DataStoreManager`, `GameLogicHandler`, `AntiCheat`) directly into Roblox Studio
3. Ensure all `RemoteEvent` and `RemoteFunction` objects are created in `ReplicatedStorage` if they do not already exist, with the exact names mapped in Step 3
4. Confirm each injection with: `[INJECTED] ServiceName/ScriptName → OK`

---

### OUTPUT FORMAT REQUIREMENTS

Structure your full response in clearly labeled sections:

```
=== STEP 1: SCAN RESULTS ===
[List all scripts found with status]

=== STEP 2: MODULE FIXES ===
[Log each fix with type: OPEN SOURCE or CUSTOM RECONSTRUCTED]

=== STEP 3: REMOTE NETWORK MAP ===
[Full table of all Remotes]

=== STEP 4: SERVER CODE ===
[Full Luau source code for each server module, clearly separated]

=== STEP 5: INJECTION LOG ===
[Confirmation of each MCP injection]
```

---

**CRITICAL EXECUTION RULES — READ BEFORE DOING ANYTHING:**

1. **Execute ONLY STEP 1 right now.** Do not write any server code, fix any modules, or generate any Remote maps yet. Output the STEP 1 scan results, then **STOP COMPLETELY** and wait for an explicit command to continue.

2. **Use MCP tools actively.** Do not simulate or hallucinate file contents. Use the MCP connection to actually read each script file from the Studio project. If a directory is empty or a script cannot be read, report that explicitly rather than fabricating content.

3. **Be exhaustive.** Do not skip scripts. Scan every descendant recursively within each target location. If a folder contains nested subfolders, scan those too.

4. **For each script found, report all three of the following — no exceptions:**
   - Full instance path (e.g., `StarterGui.FishingGui.FishingFrame.CastButton.LocalScript`)
   - Diagnosis: one or more of — `SYNTAX ERROR`, `TRUNCATED`, `MISSING RETURN`, `CLEAN`, or `UNREADABLE`
   - Classification: `OPEN-SOURCE LIBRARY`, `CUSTOM MODULE`, or `UNKNOWN`

5. **Flag ambiguous cases.** If a script's classification is uncertain (e.g., it resembles a known library but has been heavily modified), flag it as `UNCERTAIN — [reason]` and include a note on what it resembles.

6. **Do not attempt repairs in STEP 1.** Even if you identify a broken module, only log the finding. Fixes happen in STEP 2, which you will not begin until instructed.

7. **Summarize at the end of STEP 1** with a count breakdown:
   - Total scripts scanned
   - Number of `LocalScript` / `ModuleScript` / `Script` instances
   - Number flagged as broken (any non-CLEAN status)
   - Number identified as open-source libraries
   - Number identified as custom modules
   - Number classified as UNKNOWN or UNCERTAIN

**Begin STEP 1 now.**
